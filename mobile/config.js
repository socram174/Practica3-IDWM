const PORT = 3000; // Puerto del servidor
const ipv4 = ""; // Aqui va la ipv4 de tu computadora obtenida con el comando ipconfig en la cmd.
export const baseUrl = `http://${ipv4}:${PORT}/api/profile`;